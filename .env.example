# Database
DATABASE_URL="file:./dev.db" # Insert organization-specific database credentials for prod

# Auth
JWT_SECRET="0bd0725db757410704c9d06118e1bcf7"
# Set ONLY for temporary demo access to dev SSO shortcuts in production. Remove afterward.
# When set (e.g. ALLOW_DEV_SSO=1), /api/auth/sso/dev will issue tokens in production.
ALLOW_DEV_SSO=""
# When running a production build locally over http://localhost, cookies with `Secure` are rejected by the browser.
# Set ALLOW_INSECURE_LOCAL=1 to drop the Secure flag for localhost only.
ALLOW_INSECURE_LOCAL=""
# OIDC SSO (e.g., Azure AD, Auth0, Okta)
OIDC_AUTH_URL="https://example-issuer.com/oauth2/v2.0/authorize"
OIDC_TOKEN_URL="https://example-issuer.com/oauth2/v2.0/token"
OIDC_USERINFO_URL="https://example-issuer.com/oauth2/v2.0/userinfo"
OIDC_CLIENT_ID=""
OIDC_CLIENT_SECRET=""
OIDC_REDIRECT_URI="http://localhost:3000/api/auth/sso/callback"
OIDC_SCOPES="openid email profile"

# Keep-alive pinger (enable in production if your host suspends idle apps)
# When KEEPALIVE=1, the server will ping `${KEEPALIVE_PING_URL||RENDER_EXTERNAL_URL}/api/health`
# every KEEPALIVE_INTERVAL_MS (default 180000 = 3 minutes) via instrumentation.ts.
# KEEPALIVE=""
# KEEPALIVE_PING_URL="https://your-app.onrender.com"
# KEEPALIVE_INTERVAL_MS="180000"

# Storage (AWS S3 or MinIO)
S3_ENDPOINT=""
S3_REGION="us-east-1"
S3_BUCKET="school-app-media"
S3_ACCESS_KEY_ID=""
S3_SECRET_ACCESS_KEY=""

# Notifications
SENDGRID_API_KEY=""
TWILIO_ACCOUNT_SID=""
TWILIO_AUTH_TOKEN=""
TWILIO_FROM_NUMBER=""

# Sentry/LogRocket
SENTRY_DSN=""
NEXT_PUBLIC_SENTRY_DSN=""

# Client build flags
# Hide dev SSO buttons in production unless explicitly enabled
# NEXT_PUBLIC_SHOW_DEV_SSO="0"
